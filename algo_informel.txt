//BROUILLON
Ouvrir DOSSIER "Doc";
nbrFile = Compte nbr de fichier dans Doc;
Pour (i allant de 1 à nbrFile){
		Ouvrir fichier i;
		nom = getNom(i);
		Créer dossier new+i ("LSN_0"+i+"_"+nom);
		Créer fichier fichier_md (nom) dans dossier new+i
            Tant que (fichier n'est pas vide){
                String ligne = Lire fichier i; 
                écrire dans fichier_md (ligne);
            }
 		Créer fichier JSON "lesson.json" dans dossier new+i
 		écrire dans JSON 
 		"{
    "display": "LINEAR",
    "ANY": {
      "title": nom,
      "description": ""
    },
    "ENU": {
      "title": nom,
      "description": ""
    }
  }"
}

//NEW
fonction copie (nom dossier)
Ouvrir dossier "documentation"
Créer dossier ("CTG_50_docs")
nbrAction = compte nombre d'élément a parcourir;
Pour (i allant de 1 à nbrAction){
    New element i;
    Si(element i == dossier){
        name = getNom dossier
        Créez dossier ("CTG_0"+i"_"name")
        copie (element i);
    }
    Autre si (element i == png){
        copie coller image dans newFile;
    }
    autre si (element i == md){
        Ouvrir fichier i;
		nom = getNom(i);
		Créer dossier new+i ("LSN_0"+i+"_"+nom);
		Créer fichier fichier_md (nom) dans dossier new+i
            Tant que (fichier n'est pas vide){
                String ligne = Lire fichier i; 
                écrire dans fichier_md (ligne);
            }
 		Créer fichier JSON "lesson.json" dans dossier new+i
 		écrire dans JSON 
 		
    }

}

"{
    "display": "LINEAR",
    "ANY": {
      "title": nom,
      "description": ""
    },
    "ENU": {
      "title": nom,
      "description": ""
    }
  }"

// V3
Déclarer la variable "répertoire origine"
Déclarer la variable "répertoire cible"
Créer la catégorie "CTG_50_docs" et son fichier json dans le répertoire cible
Ouvrir le dossier "répertoire origine"
Pour chaque fichier du dossier "répertoire origine"
    Si c'est un dossier
        Lire le nom du dossier (du type "01-core")
        Détecter le nom de la catégorie ("core")
        Créer la catégorie (type "CTG_10_core") et json fichier json dans "CTG_50_docs"
        Pour chaque fichier du dossier
        Si c'est un fichier ".md"
            Lire le nom du fichier ".md" (du type basic-code-examples.md)
            Détecter le nom de la catégorie (Basic code example)
            Créez la leçon (type "LSN_01_basics-code-examples) avec son fichier "basic-code-examples.md" et son fichier json
    Si c'est un fichier ".md"
        Lire le nom du fichier ".md" (du type architecture.md)
        Détecter le nom de la catégorie (Architecture)
        Créez la leçon (type "LSN_01_architecture) avec son fichier "LSN_01_architecture.md" et son fichier json
        Regarder dans le dossier s'il possède des fichiers ".png" en faisant une recherche par nom, si oui les déplacer vers sa "lesson correspondante"
         
    



